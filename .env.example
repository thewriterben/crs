# Docker Compose Environment Variables

# PostgreSQL Configuration
POSTGRES_USER=cryptons_user
POSTGRES_PASSWORD=crs_password_change_me
POSTGRES_DB=cryptons_db

# Backend Configuration
FLASK_ENV=production
SECRET_KEY=your-secret-key-here-minimum-32-characters-long
JWT_SECRET_KEY=your-jwt-secret-key-here-minimum-32-characters

# CORS Settings
CORS_ORIGINS=http://localhost:8080,http://localhost:80

# Frontend Configuration
VITE_API_URL=http://localhost:5000

# Optional: External Services
# REDIS_URL is automatically configured in docker-compose.yml
# DATABASE_URL is automatically configured in docker-compose.yml
