[pytest]

# Output options
addopts = 
    -v
    --strict-markers
    --tb=short
    --cov=src
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
omit = 
    */tests/*
    */venv/*
    */__pycache__/*
    */site-packages/*

[coverage:report]
